<UserControl
    x:Class="ChatClient.Views.FriendTabViews.AllFriendsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ChatClient.Views.FriendTabViews"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:self="clr-namespace:ChatClient.ViewModels.FriendsTabViewModels"
    d:DataContext="{d:DesignInstance Type=self:AllFriendsView,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <Label
                Content="All Friends ~ 5"
                FontSize="14"
                FontWeight="Bold" 
                Margin="15,0,0,0"/>
            <ListView ItemContainerStyle="{StaticResource FriendsCard}" ItemsSource="{Binding FriendList}" />
        </StackPanel>
    </Grid>
</UserControl>
